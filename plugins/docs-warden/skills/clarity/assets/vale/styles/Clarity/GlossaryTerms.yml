# Generated by glossary_to_vale.py from the repository's docs/GLOSSARY.md.
# The shipped file is empty on purpose: approved and rejected terms come only
# from each repo's own glossary, never from a list bundled with the skill.
extends: substitution
message: "Use '%s' instead of '%s' (docs/GLOSSARY.md)."
level: error
ignorecase: true
swap: {}
