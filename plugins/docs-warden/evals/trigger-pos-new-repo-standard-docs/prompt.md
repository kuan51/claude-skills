---
tags: [trigger-positive]
allowed_tools: [Read, Glob, Grep, Skill]
max_turns: 30
expected_outcome: the docs-warden skill is invoked
---

New repo for the cert-rotation tooling: a PowerShell module plus a couple of scripts. Set up the standard docs — README, conventions, the decisions folder, security policy, all that — the way we do it everywhere else.
