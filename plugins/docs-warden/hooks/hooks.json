{
  "description": "docs-warden -- at session start, says when docs/decisions has reached the compaction point so the compact mode gets run.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/decisions_check.py\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
