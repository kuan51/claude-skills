# Copy to the repository root. Run `vale sync` once to fetch the packages.
StylesPath = styles
MinAlertLevel = suggestion

# Front matter title and description are not linted. In a skill or agent file
# the description decides when it is triggered, so rewording it to satisfy a
# prose rule changes behavior. Vale requires this as a core option, above any
# [section]. Setting IgnoredScopes replaces Vale's default rather than adding to
# it, so `code, tt` (inline code) must be listed again or code spans get linted.
IgnoredScopes = code, tt, text.frontmatter.title, text.frontmatter.description

Vocab = Project

# proselint and ai-tells are pinned by release URL. ai-tells rules default to
# error and new releases add rules, so an unpinned sync could start blocking
# commits without any change here.
Packages = Microsoft, write-good, \
  https://github.com/vale-cli/proselint/releases/download/v0.3.4/proselint.zip, \
  https://github.com/tbhb/vale-ai-tells/releases/download/v1.35.0/ai-tells.zip

[*.md]
BasedOnStyles = Clarity, Microsoft, write-good, proselint, ai-tells

# Rules switched off deliberately. Each was observed firing on real project
# documentation and judged wrong for this context, not merely noisy.
#
# Contractions: it wants "didn't" for "did not". In a safety instruction the
#   contraction is weaker and easier to misread. This one would do harm.
# Terms: Microsoft's product vocabulary. It reads our "agent" as shorthand for
#   "personal digital assistant".
# We: our CONVENTIONS and decision records are deliberately first person plural;
#   naming who decided is the point of a decision record.
# E-Prime: bans every form of "to be". A whole-language prohibition nobody is
#   going to follow, and following it would make technical prose worse.
Microsoft.Contractions = NO
Microsoft.Terms = NO
Microsoft.We = NO
write-good.E-Prime = NO

# Both packages default to warning. Vale takes a bare style name as the level for
# every rule in it, and a Style.Rule line beside it overrides one rule. The rules
# promoted to error are the ones that fired during the 0.3.0 doc pass and never
# flagged accurate wording: punctuation and filler, whose fix cannot change what a
# sentence says. Rules that never fired stay at warning until they prove precise.
proselint = warning
ai-tells = warning
ai-tells.EmDashUsage = error
ai-tells.DoubleHyphen = error
ai-tells.ColonUsage = error
ai-tells.SemicolonUsage = error
ai-tells.ShipOveruse = error
ai-tells.FillerIntensifier = error
ai-tells.FillerPhrases = error
ai-tells.HedgingPhrases = error
ai-tells.ConclusionMarkers = error
ai-tells.RestatementMarkers = error
ai-tells.CataphoricForecasting = error
ai-tells.LabelAndExplain = error
ai-tells.EmphaticCopula = error
ai-tells.ColloquialAssessments = error
ai-tells.ExplainerLeads = error
proselint.Uncomparables = error
proselint.CorporateSpeak = error
Microsoft.DateOrder = warning

# `Style = level` / `Style.Rule = level` in [*.md] applies to every section.
# Mirror the promotion list as NO in each section below.
#
# History is not rewritten to satisfy a linter. Decision records are immutable
# once accepted, run logs are append-only, and changelogs and dated plans record
# what was true then. Whole-file linting would force edits to old entries.
# Test fixtures are sample repositories the docs-warden tests read, and the
# docs-warden eval prompts are test inputs quoted word for word; rewording either
# changes what a test checks. These keep the base styles.
[{docs/decisions/**/*.md,**/RUNLOG.md,**/CHANGELOG.md,docs/superpowers/**/*.md,plugins/docs-warden/test/fixtures/**/*.md,plugins/docs-warden/evals/README.md}]
BasedOnStyles = Clarity, Microsoft, write-good
proselint = NO
ai-tells = NO
ai-tells.EmDashUsage = NO
ai-tells.DoubleHyphen = NO
ai-tells.ColonUsage = NO
ai-tells.SemicolonUsage = NO
ai-tells.ShipOveruse = NO
ai-tells.FillerIntensifier = NO
ai-tells.FillerPhrases = NO
ai-tells.HedgingPhrases = NO
ai-tells.ConclusionMarkers = NO
ai-tells.RestatementMarkers = NO
ai-tells.CataphoricForecasting = NO
ai-tells.LabelAndExplain = NO
ai-tells.EmphaticCopula = NO
ai-tells.ColloquialAssessments = NO
ai-tells.ExplainerLeads = NO
proselint.Uncomparables = NO
proselint.CorporateSpeak = NO

# The glossary lists its own rejected terms in the "Do not use" column, so the
# substitution rule would flag the source it was generated from.
[docs/GLOSSARY.md]
Clarity.GlossaryTerms = NO

# Fully generated documents: linting a file a script owns only annoys the
# script's author. Keep this list in step with generated_docs entries that
# cover the WHOLE file. A partially generated document (e.g. README.md, where
# only the plugin table between BEGIN/END GENERATED markers is generated)
# stays linted - the hand-written prose around the generated region still
# needs it, and exempting the file would silently disable linting on the rest.
# The clarity skill's worked examples quote bad prose on purpose.
[{docs/DECISIONS.md,docs/decisions/README.md,docs/regulatory/traceability-matrix.md,plugins/docs-warden/skills/clarity/examples/before-after.md}]
BasedOnStyles = ""
proselint = NO
ai-tells = NO
ai-tells.EmDashUsage = NO
ai-tells.DoubleHyphen = NO
ai-tells.ColonUsage = NO
ai-tells.SemicolonUsage = NO
ai-tells.ShipOveruse = NO
ai-tells.FillerIntensifier = NO
ai-tells.FillerPhrases = NO
ai-tells.HedgingPhrases = NO
ai-tells.ConclusionMarkers = NO
ai-tells.RestatementMarkers = NO
ai-tells.CataphoricForecasting = NO
ai-tells.LabelAndExplain = NO
ai-tells.EmphaticCopula = NO
ai-tells.ColloquialAssessments = NO
ai-tells.ExplainerLeads = NO
proselint.Uncomparables = NO
proselint.CorporateSpeak = NO
